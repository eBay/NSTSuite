openapi: 3.0.0
info:
  title: 400 response status
  description: >-
    400 response status
  version: 1.0.0
paths:
  /test:
    get:
      summary: placeholder summary
      responses:
        '400':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
components:
  schemas:
    Response:
      type: object
      properties:
        hello:
          type: string