package com.ebay.service.logger.platforms.util;

/**
 * These are the keys (AKA column headers) for the csv files parsed for the NST
 * to FUI platform mappings.
 */
public enum PlatformApiToFuiMappingKeys {
  SERVICE,
  API,
  PLATFORM_REQUEST_TYPE,
  NAVIGATION,
  ENTRY,
  IMPORTS,
  MEMBER_FIELDS
}
